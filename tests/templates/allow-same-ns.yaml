apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ input['name'] }} 
  namespace: {{ input.get('namespace', 'default') }}
spec:
  ingress:
  - from:
    - podSelector: {}
  podSelector: {}
  policyTypes:
  - Ingress