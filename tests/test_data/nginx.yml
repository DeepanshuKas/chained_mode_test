apiVersion: v1
kind: Pod
metadata:
  name: my-containerport-pod
  labels:
#    app: myapp
    app: nginx
spec:
  containers:
  - name: myapp-container
    image: noiro-quay.cisco.com/noiro/nginx
    ports:
    - containerPort: 80
    imagePullPolicy: IfNotPresent
