apiVersion: v1
kind: Pod
metadata:
  name: pd-validator
  labels:
    test: port-distribution-validator
  namespace: port-distribution-ns
spec:
  containers:
  - image: noiro-quay.cisco.com/noiro/alp-curl
    command:
      - sleep
      - "3600"
    imagePullPolicy: IfNotPresent
    name: alpine
  restartPolicy: Always
  terminationGracePeriodSeconds: 0
