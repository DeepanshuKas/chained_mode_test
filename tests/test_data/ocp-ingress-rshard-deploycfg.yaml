apiVersion: apps/v1
kind: Deployment
metadata:
  name: print-headers
spec:
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: print-headers
  strategy:
    resources: {}
    rollingParams:
      intervalSeconds: 1
      maxSurge: 25%
      maxUnavailable: 25%
      timeoutSeconds: 600
      updatePeriodSeconds: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: print-headers
    spec:
      containers:
      - image: noiro-quay.cisco.com/noiro/quarkus-print-headers:latest
        imagePullPolicy: IfNotPresent
        name: print-headers
        ports:
        - containerPort: 8080
          protocol: TCP
        resources: {}
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
  test: false
  triggers:
  - type: ConfigChange
