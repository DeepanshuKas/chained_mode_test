apiVersion: v1
kind: Service
metadata:
  labels:
    app: docker-demo
  name: docker-demo
  namespace: ci-sddc2465-pod-scaling
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: docker-demo
