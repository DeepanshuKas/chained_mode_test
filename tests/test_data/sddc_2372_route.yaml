apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: myjavaapp
  namespace: ci-s2i-build-test
  labels:
    app: myjavaapp
spec:
  path: /myjavaapp
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: myjavaapp
